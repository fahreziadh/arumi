# Written automatically by `bunx convex dev`; listed here for reference.
VITE_CONVEX_URL=

# Everything else (OPENROUTER_API_KEY, ADMIN_EMAIL, AUTH_GOOGLE_ID,
# AUTH_GOOGLE_SECRET, SITE_URL) lives on the Convex deployment:
#   bunx convex env set NAME value
